"use strict";
let userName = ['admin', 'Hamza', 'Ahmed', 'Arham', 'Amir'];
if (userName.length === 0) {
    console.log('we need to find some user');
}
else {
    userName = [];
    console.log('All user NAme have been removed. ' + userName.length);
}
